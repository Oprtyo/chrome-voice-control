# Voice Control / Голосовое управление Chrome

Управляйте браузером Chrome голосовыми командами на русском языке.  
Скажите **«Окей браузер»** — и дайте команду.

## Как это работает

1. Python-скрипт с **Vosk** постоянно слушает микрофон (локально, офлайн)
2. При фразе **«Окей браузер»** активируется распознавание команды
3. Команда передаётся в Chrome-расширение через WebSocket
4. Расширение выполняет действие в браузере

Вся обработка речи происходит **локально** — аудио не отправляется в интернет.

## Установка

### 1. Установите зависимости

**Windows:**
```
install.bat
```

**Linux / macOS:**
```bash
chmod +x install.sh start.sh
./install.sh
```

Скрипт установит Python-библиотеки и скачает модель Vosk (~45 МБ).

### 2. Установите Chrome-расширение

1. Откройте `chrome://extensions/`
2. Включите **Режим разработчика** (правый верхний угол)
3. Нажмите **Загрузить распакованное расширение**
4. Выберите эту папку (`chrome-voice-control`)

### 3. Запустите сервер

**Windows:** запустите `start.bat`  
**Linux / macOS:** запустите `./start.sh`

В popup расширения появится зелёный индикатор «Сервер подключён».

## Использование

1. Убедитесь, что сервер запущен (терминал с `voice_server.py`)
2. Скажите **«Окей браузер»** — услышите подтверждение на странице
3. Произнесите команду (например: «прокрути вниз», «новая вкладка»)
4. Расширение выполнит команду

## Голосовые команды

| Команда | Действие |
|---------|----------|
| **назад** | Назад в истории |
| **вперёд** | Вперёд в истории |
| **обновить** | Перезагрузить страницу |
| **домой** | Новая вкладка |
| **вверх / вниз** | Прокрутка |
| **влево / вправо** | Горизонтальная прокрутка |
| **в начало / в конец** | К верху / низу страницы |
| **новая вкладка** | Открыть новую вкладку |
| **закрыть вкладку** | Закрыть текущую |
| **следующая / предыдущая вкладка** | Переключение |
| **найди [запрос]** | Поиск в Google |
| **открой [сайт]** | Открыть сайт |
| **увеличить / уменьшить** | Зум страницы |
| **нажми** | Клик по центру экрана |

## Требования

- **Python 3.8+**
- **Google Chrome 110+**
- **Микрофон**
- Интернет для первой установки (скачивание модели)

## Структура проекта

```
chrome-voice-control/
├── manifest.json          # Chrome extension manifest
├── background/            # Service worker (WebSocket клиент)
├── content/               # Content script (действия на странице)
├── popup/                 # UI расширения
├── icons/                 # Иконки
├── server/
│   ├── voice_server.py    # Python сервер (Vosk + WebSocket)
│   ├── requirements.txt   # Python зависимости
│   └── model/             # Модель Vosk (после установки)
├── install.bat / .sh      # Скрипт установки
├── start.bat / .sh        # Скрипт запуска
└── README.md
```

## Лицензия

MIT
